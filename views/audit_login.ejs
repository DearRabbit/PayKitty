<!DOCTYPE html>
<html lang="zh-CN">
  <head>
    <% include audit_header.ejs %>
  </head>
  <body style="background:#f7f7f7;">
    <div class="">
      <div id="wrapper">
        <div id="login" class=" form">
          <section class="login_content">
            <form action="" method="post" onsubmit="return checkNull();">
              <h1>审计系统登录</h1>
              <% if (login_fail) { %>
              <div class="alert">
                <a class="close" data-dismiss="alert">×</a>
                <strong>登录错误!</strong> 用户名或密码错误
              </div>
              <% } %>
              <div>
                <input name="username" type="text" class="form-control" placeholder="用户名" required/>
              </div>
              <div>
                <input name="password" type="password" class="form-control" placeholder="密码" required/>
              </div>
              <button type="submit" class="btn btn-default submit">登录</button>
              <div class="separator">
                <h1><i class="fa fa-leaf" style="font-size: 26px;"></i> PayKitty</h1>
              </div>
            </form>
          </section>
        </div>
      </div>
    </div>
    <% include audit_footjs.ejs %>
    <script>function checkNull(){if($(":text")[0].value==""){alert("用户名不能为空");return false}else{if($(":password")[0].value==""){alert("密码不能为空");return false}}};</script>
  </body>
</html>
